import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.Mexpense',
  appName: 'Mexpense',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
